#if !defined(GRAINS_H)
#define GRAINS_H
#include <bitset>
#include <cmath>
#define ull unsigned long long

namespace grains {
    ull square(int);

    ull total();
}  // namespace grains

#endif // GRAINS_H
